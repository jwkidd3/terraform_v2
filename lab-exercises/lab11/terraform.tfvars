# terraform.tfvars
#
# Nothing is set here. For Terraform Cloud labs, set `username`, `environment`,
# `instance_count`, and `aws_region` as Terraform Variables in EACH workspace
# (TFC remote runs do not inherit local TF_VAR_* env vars).
#
# See Exercise 11.2 Step 3 for the full list of workspace variables.
